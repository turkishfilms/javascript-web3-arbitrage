import { coinData, exchangeData } from './constants'
import { relevantAPIs } from './apis'
import scan from './Arb2'

/**
 * Grab coins and exchanges
 *
 * After a certain interval, scan chosen coin prices
 *
 * Find most profitable trades
 *
 * Execute trades
 *
 * Buy a celebratory lambo
 * **/

if ("data is 'too' old") {
	const have = "API GRAB FRESH DATA"
} else {
	const use = "THE CURRENT FILES ON HAND"
}

//after that
setTimeout(INTERVAL, () => {
	const prices = scan("prices")
	//save price data
})

function getMegaData() {
	fetch("https://github.com/turkishfilms/general-particle-system")
}
